// swift-tools-version: 5.6

import PackageDescription

let package = Package(
    name: "${PACKAGE}",
    platforms: [
        .iOS(.v14),
    ],
    products: [
        .library(
            name: "${PACKAGE}",
            targets: ["${PACKAGE}"]
        ),
    ],
    dependencies: [],
    targets: [
        .target(
            name: "${PACKAGE}",
            dependencies: []
        ),
    ]
)
