import Root // NOTE: available by configuring this package as a framework
import SwiftUI

@main
struct TraqCloneApp: App {
    var body: some Scene {
        WindowGroup {
            RootView()
        }
    }
}
