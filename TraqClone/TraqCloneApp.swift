import SwiftUI
import Views // NOTE: available by configuring this package as a framework

@main
struct TraqCloneApp: App {
    var body: some Scene {
        WindowGroup {
            RootView()
        }
    }
}
